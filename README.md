# FormValidator
Form validation using VanillaJS
This project contains Web forms through which we can validate the users.
The web form contains the frontend HTML-CSS files for View and vanillaJS as a backend for validations.
